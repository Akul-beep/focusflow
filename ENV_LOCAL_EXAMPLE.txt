# Default: Groq (all app AI calls use this when set)
GROQ_API_KEY=gsk_PASTE_YOUR_KEY_HERE
GROQ_MODEL=llama-3.3-70b-versatile
# Optional model-level fallback(s), comma-separated (tried automatically on Groq failures).
# Leave unset to avoid lower-quality models; only add if you explicitly want fallbacks.
# GROQ_MODEL_FALLBACKS=

# BYOK: encrypt per-user Groq keys stored in Supabase (min 16 chars; use a long random secret in production)
AI_USER_KEY_ENCRYPTION_SECRET=replace_with_a_long_random_secret_string

# Max shared-server AI requests per signed-in user per UTC day (0 = unlimited shared usage counting)
AI_SHARED_DAILY_LIMIT=5
# Optional: cap Groq max output tokens (default 4096). Lower if you hit TPM limits on free tier.
# GROQ_MAX_OUTPUT_TOKENS=3072

# Legacy / optional: Google Gemini only if you set AI_PROVIDER=gemini
# AI_PROVIDER=gemini
# GEMINI_API_KEY=PASTE_YOUR_KEY_HERE
# GEMINI_MODEL=gemini-2.5-flash-lite

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

